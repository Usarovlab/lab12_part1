#include<iostream>
#include<fstream>
#include<string>
using namespace std;
int main() {
    ifstream fout("../text.txt");
    string line;
    int count=0;
    while (getline(fout,line))
        count++;
    fout.close();
    cout<<count<<endl;
    return 0;
}
