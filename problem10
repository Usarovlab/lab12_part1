#include<iostream>
#include<fstream>
using namespace std;
int main() {
    ifstream fout("../text.txt");
    string line;
    int count=0;
    char c;
    while (fout.get(c))
        count++;
    fout.close();
    cout<<count;
    return 0;
}
