#include <iostream>
#include<fstream>
using namespace std;
int main() {
    ifstream  azz("../input.txt");
    int x, sum=0;
    while (azz>>x) {
        sum+=x;
    }
    cout<<sum<<endl;
    azz.close();
    return 0;

}
